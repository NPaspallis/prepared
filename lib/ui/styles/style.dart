import 'package:flutter/material.dart';

//Paddings
const EdgeInsets screenPadding = EdgeInsets.all(5);
const EdgeInsets standardPadding = EdgeInsets.all(10);
const EdgeInsets bottomNavigationPadding = EdgeInsets.only(left: 5, right: 5, top: 5, bottom: 5);
const EdgeInsets buttonPadding = EdgeInsets.only(left: 3, right: 3);

//Colors
const Color primaryColor = Colors.teal;
const Color secondaryColor = Colors.lightGreen;
Color backgroundColor = Colors.grey.shade300;

//Font sizes
const double normalTextSmall = 16;
const double normalTextBig = 18;
const double normalTextLarge = 20;
const double subtitlesFontSize = 12;

//Other
const double cursorHeight = 25;

//Buttons:
const TextStyle elevatedButtonTextStyle = TextStyle(
  color: Colors.white,
  fontSize: 20,
  fontWeight: FontWeight.bold
);